# Google Play API Credentials path
GOOGLE_PLAY_CREDENTIALS_PATH=/path/to/your/credentials.json

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Storage directory for JSON files
STORAGE_DIR=data

# Use mock data instead of real API (true/false)
USE_MOCK_DATA=false